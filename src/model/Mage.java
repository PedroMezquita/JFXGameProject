package model;

public class Mage extends Personnage {



    public Mage(int atkPnt, int magPnt, int maxHP, int maxMana, int currentHP, int currentMana, int speed) {
        super(atkPnt, magPnt, maxHP, maxMana, currentHP, currentMana, speed);
    }
}
